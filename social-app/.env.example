# API配置 (API Configuration)
# ================================
# 推荐使用项目根目录的 .eov 文件进行统一配置
# Recommended to use .eov file in project root for centralized configuration
#
# .eov 文件优势:
# - 统一管理APK和Web的API地址
# - 修改一处即可更新所有应用
# - 运行 npm run build 或 npm start 后生效
#
# 如果仍需使用环境变量，可以配置以下值（会覆盖.eov设置）:
# If you still want to use environment variables, configure the following (will override .eov):
REACT_APP_API_URL=http://103.74.193.179:5000
